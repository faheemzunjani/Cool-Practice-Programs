#include <stdio.h>

struct node {
	int info;
	struct node * next;
};

struct node * insertion_sort(struct node * head);

int main()
{
	struct node * head = NULL;
	
	return 0;
}

struct node * insertion_sort(struct node * head)
{
}
